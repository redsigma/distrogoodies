---

#
# File containing good to know approaches
#

- name: check packages
  package_facts:
    manager: auto

- name: print packages
  debug:
    msg: "Packages >> {{ ansible_facts.packages }}"
  when: "'NetworkManager' in ansible_facts.packages"
